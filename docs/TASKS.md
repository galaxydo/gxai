# gx402 — Tasks & Ideas

## 🔴 Priority: Fix
- [x] ~~**Stream tool invocation outputs**~~ — ✅ DONE. `Agent.run` now successfully emits explicit `progressCallback` records with `data: parameters` mapped immediately before runtime MCP execution, and maps `data: result` as soon as the execution responds, enabling rich terminal trace logs for tool activity.
- [x] ~~**Test coverage for LoopAgent**~~ — ✅ DONE. Added `test/loop.test.ts` to test `write_file`, `read_file`, and `exec` tools along with outcome predicate logic, simulating multi-step reasoning natively.
- [x] ~~**Validate Provider API Keys**~~ — ✅ DONE. Explicit API key existence checks have been added for Anthropic, DeepSeek, Google, and OpenAI in `inference.ts`. Additionally, I moved the input `Zod` validation outside of `measure.assert` in `Agent.run` to guarantee early rejection before attempting inference, which also fixed 3 currently failing unit tests.

## 🟡 Priority: Improve
- [x] ~~**OpenAI native Structured Outputs**~~ — ✅ DONE. `response_format: { type: "json_schema" }` is directly synthesized from output schemas via `zod-to-json-schema` and sent on the `gpt` and `o4-` family configurations implicitly guaranteeing correct output without the verbose custom XML prompting instruction payload overhead.
- [x] ~~**Local Analytics Viewer**~~ — ✅ DONE. `bun run analytics` (or `gx --analytics` through the main binary) triggers a beautifully formatted colorized terminal output UI listing all offline-queued telemetry logs stored in `~/.gxai/analytics_queue.json`, supporting truncations and direct queue flushing via `--clear`.
- [x] ~~**Analytics Dashboard Polish**~~ — ✅ DONE. The `sendAnalytics` function in `agent.ts` now uses an offline queue (`~/.gxai/analytics_queue.json`) to persist inference telemetry if the `analyticsUrl` endpoint is unreachable. The queue is automatically flushed on the next successful telemetry request, ensuring no data loss during network outages or dashboard downtime. Tests are running cleanly in `test/analytics.test.ts`.
- [x] ~~**Streaming nested object support**~~ — ✅ DONE. Verified engine perfectly handles arbitrary deep object nesting (> 3 levels) thanks to `tagStack.join("_")` inside the realtime XML progressive chunk parser, and added an explicit recursive boundary mock test in `test/streaming.test.ts` to guarantee it never regresses.

## 🟢 Priority: Features
- [x] ~~**Parallel Tool Execution**~~ — ✅ DONE. Refactored both `Agent.run` and `LoopAgent.execute` core execution loops to tear down synchronous linear blockings. Instead of iterating `relevantServers` -> `relevantTools` and awaiting them one-by-one, the engine now eagerly synthesizes parameters and executes external IO network requests wrapped efficiently into uncoupled closures fed directly into `Promise.all()`. This dramatically accelerates total time-to-first-token latency when the agent constructs massive horizontal multi-tool fanout operations.
- [x] ~~**WebSockets for MCP**~~ — ✅ DONE. Upgraded the generic HTTP `fetchWithPayment` utility to safely intercept `ws://` and `wss://` protocols. Connections are instantly upgraded to native runtime WebSockets and multiplexed seamlessly into `Map`-backed connection pools per origin, converting classic REST endpoints implicitly into low-latency JSON-RPC over WS. It faithfully synthesizes a `Response` mock to ensure backward compatibility across the rest of the generic inference framework codebase without rewriting tool clients.
- [x] ~~**x402 Server Identity Validation**~~ — ✅ DONE. Added `allowedRecipients?: string[]` to the Agent's `solanaWallet` configuration parameter, letting developers explicitly dictate a hardened registry. The `fetchWithPayment` flow now cross-matches the `recipient` from the HTTP 402 body against this list (if provided), halting execution unconditionally and throwing a Security Error to prevent accidental spoofing.
- [x] ~~**Claude 3.5 Sonnet Support**~~ — ✅ DONE. Explicitly added `claude35Sonnet` and `claude35SonnetLatest` into the global `LLM` exported types. Furthermore, refactored Anthropic inference adapter in `src/inference.ts` to properly segregate the `.find(m => m.role === "system")` prompt out of the `messages` array into the root `system` string parameter as demanded by the Anthropic message API spec.
- [x] ~~**Solana Transaction Retries for 402**~~ — ✅ DONE. Upgraded the static `connection.sendTransaction` logic in `src/payments.ts` to use `measure.retry` wrapping blockhash extraction, submission, and confirmation into one self-repairing chunk that automatically triggers `exponential backoff` (3 attempts, 2-second base delay, 2x scaling per iteration).
- [x] ~~**Advanced Tool Auth Hooks**~~ — ✅ DONE. Allowed developers to inject an asynchronous `authorize?: (params: any) => Promise<boolean | string>` callback directly into `MCPTool` configurations via the new `tools` override mapped directly into the `MCPServer` constructor. The `Agent.run` loop evaluates this intercept automatically. If the hook returns anything other than `true`, the host execution short-circuits with an explicitly synthesized `{ error: string }` payload rather than triggering an invocation.
- [x] ~~**Agent State Serialization**~~ — ✅ DONE. Upgraded `LoopAgent` to maintain its `LoopState` as a persistent class instance property. Implemented `.toJSON()` to dump the iteration cursor and full `toolHistory`, and a static `.fromJSON()` factory to seamlessly orchestrate the resurrection of an agent's context across process boundaries.

## 📝 Architecture Notes
- **Core Abstractions**: `Agent` (single-shot/streaming) and `LoopAgent` (iterative tool-use).
- **Inference**: Uses provider-specific APIs with unified streaming output mapped via Zod.
- **Multimodal**: Gemini capabilities (`generateImage`, `generateVideo`, `generateMusic`) integrated directly.
- **Payments**: Auto-pays HTTP 402 responses via Solana (x402 protocol) if a wallet and MCP server are configured.
- **Testing**: Bun-native test runner (`bun test`).

## 🔮 Ideas / Backlog
- [x] ~~**Token Usage Tracking**~~ — ✅ DONE. `callLLM` now extracts token usage from all 4 provider response formats (OpenAI `usage.prompt_tokens`, Anthropic `usage.input_tokens`, Gemini `usageMetadata.promptTokenCount`, DeepSeek) via the unified `extractUsage()` normalizer. Exposed as `lastTokenUsage` export for per-call access. `Agent.run` accumulates usage across all internal LLM calls (server selection, tool params, response generation) into `agent.lastUsage: TokenUsage`. Analytics payload now includes optional `tokenUsage` field. Types: `TokenUsage { inputTokens, outputTokens, totalTokens }` and `LLMResult` exported from package index.
- [x] ~~**Multi-turn Conversation Pruning**~~ — ✅ DONE. Added `contextWindow` config (default 20) to `LoopConfig`. `buildIterationPrompt` now splits tool history into a `summarizeHistory()` compact digest (tool counts + success rates) for older calls, and verbatim detail for the most recent `contextWindow` calls. Tool output truncation reduced from 500→300 chars. Prevents LoopAgent from hitting token limits on long-running loops while preserving recent context fidelity.
- [x] ~~**Provider Fallback Chain**~~ — ✅ DONE. New `callLLMWithFallback(fallback, messages, options)` sequentially tries each provider in a `FallbackConfig.providers` chain. On failure (rate limit, timeout, API error), logs a warning and tries the next. Optional `onFallback` callback notifies callers. All types exported from package index: `callLLMWithFallback`, `FallbackConfig`.
- [x] ~~**Cost Estimation per Run**~~ — ✅ DONE. New `src/pricing.ts` module with per-model pricing table for all 4 providers (OpenAI, Anthropic, DeepSeek, Gemini) in USD per million tokens. `Agent.estimateCost(input)` serializes input to XML, estimates token count from char length (~4 chars/token), and returns `CostEstimate { inputCost, outputCost, totalCost, currency }`. `Agent.lastCost` is auto-populated from actual token usage after each `run()`. Exported: `getModelPricing`, `calculateCost`, `estimateInputCost`, `ModelPricing`, `CostEstimate`.
- [x] ~~**LoopAgent Checkpointing**~~ — ✅ DONE. New `checkpointPath` config in `LoopConfig`. After each iteration, `saveCheckpoint()` writes `state.toJSON()` to disk (creates parent dirs automatically). On success, `removeCheckpoint()` cleans up the file. Static `LoopAgent.fromCheckpoint(path, config)` factory resumes from a checkpoint file. Checkpoint writes are non-fatal — failures are silently swallowed to avoid breaking the agent loop.
- [x] ~~**Streaming Token Usage**~~ — ✅ DONE. OpenAI/DeepSeek: added `stream_options: { include_usage: true }` to streaming request bodies so the final SSE chunk includes `usage`. Anthropic: extracts `input_tokens` from `message_start` event and `output_tokens` from `message_delta` event (natively provided). The `parseSseLine` function now accumulates usage into a `streamUsage` object alongside content parsing, assigned to `lastTokenUsage` after the stream completes. Token tracking now works for both streaming and non-streaming calls.
- [x] ~~**Agent Middleware/Hooks**~~ — ✅ DONE. `agent.use(fn)` registers `AgentMiddleware` callbacks. Hooks fire at 3 phases: `before` (pre-execution, can throw to abort), `after` (post-success with output/usage/cost/duration), `error` (on failure with error details). `MiddlewareContext { phase, agentName, llm, input, output?, error?, usage?, cost?, durationMs? }`. Multiple middleware execute in registration order. `use()` returns `this` for chaining. Types exported: `AgentMiddleware`, `MiddlewareContext`.
- [x] ~~**Token Budget Guard**~~ — ✅ DONE. New `maxCostUSD` field on `AgentConfig`. At the start of `run()`, calls `estimateCost()` against the configured budget. If estimated cost exceeds `maxCostUSD`, throws `Budget exceeded: estimated cost $X exceeds maxCostUSD $Y for model "Z"` before any LLM calls are made. Uses `maxTokens` (or default 4000) as the estimated output token count.
- [x] ~~**Response Caching**~~ — ✅ DONE. New `src/cache.ts` with `cachedCallLLM()` — in-memory LRU cache keyed by FNV-1a hash of (model + messages + options). Configurable `CacheConfig { ttlMs, maxEntries }` (defaults: 5min, 100 entries). Auto-evicts expired entries and enforces max size. Streaming calls pass through uncached. Utilities: `clearCache()`, `getCacheSize()`, `getCacheStats()`. All exported from package index.
- [x] ~~**LoopAgent Event Streaming**~~ — ✅ DONE. Two new methods on `LoopAgent`: `executeAsSSE(task)` returns a `ReadableStream<Uint8Array>` that JSON-encodes each `LoopEvent` as `data:` SSE frames, closing with `[DONE]` sentinel. `createSSEResponse(task)` returns a complete `Response` with proper SSE headers (`Content-Type: text/event-stream`, `Cache-Control: no-cache`, `Connection: keep-alive`). Can be returned directly from API route handlers for real-time dashboard integration.
- [x] ~~**Agent Conversation Memory**~~ — ✅ DONE. New `src/memory.ts` with `ConversationMemory` class. Maintains prunable message history with configurable `maxTurns` (default 20). Methods: `addUser()`, `addAssistant()`, `getMessages()`, `getContextString()` (XML-formatted), `toJSON()`/`fromJSON()` for serialization, `clear()`. Integrated with Agent via `AgentConfig.memory`: auto-records user input (XML) and assistant output after each `run()`, injects history into system prompt via `getContextString()` when `turnCount > 1`. Exported: `ConversationMemory`, `ConversationMemoryConfig`, `MemoryMessage`.
- [x] ~~**Tool Authorization Audit Log**~~ — ✅ DONE. New `src/audit.ts` with `AuditLog` class — in-memory circular buffer (default 1000 entries) logging every tool authorization decision. Records: timestamp, decision (allow/deny), tool, server, agentName, reason, parameters. Query: `getEntries({ since, decision, tool, server, limit })`. Stats: `getStats()` → { totalEntries, allowCount, denyCount, deniedTools }. Integrated into `Agent.run()` at tool invocation: logs 'deny' with reason when `authorize` rejects, 'allow' when it passes or when no hook exists. Global `auditLog` singleton exported. Types: `AuditEntry`, `AuditQuery`, `AuditStats`.
- [x] ~~**Batch Agent Execution**~~ — ✅ DONE. New `agent.runBatch(inputs, { concurrency })` processes multiple inputs in parallel using chunked `Promise.allSettled()`. Default concurrency: 5. Returns `{ results: O[], errors: Array<{ input, error }> }`. Failed inputs don't block others — partial results are always returned.
- [x] ~~**Agent Retry with Backoff**~~ — ✅ DONE. New `agent.runWithRetry(input, { maxRetries, retryDelayMs, maxDelayMs })`. Retries failed `run()` calls with exponential backoff (`delay * 2^attempt`, capped at `maxDelayMs`). Smart non-retry: budget exceeded, validation, and parse errors throw immediately without retrying. Defaults: 3 retries, 1s base delay, 30s max delay.
- [x] ~~**Agent Cloning**~~ — ✅ DONE. New `agent.clone(overrides?)` creates a new Agent with shallow-merged config and copies all registered middleware. Enables creating model/temperature variants from a base agent without re-registering hooks.
- [x] ~~**Structured Error Types**~~ — ✅ DONE. New `src/errors.ts` with typed error hierarchy rooted at `GxaiError`. Five classes: `BudgetExceededError` (estimatedCost, maxCostUSD, model), `ValidationError` (zodErrors), `ProviderError` (provider, statusCode, retryable flag), `AuthorizationError` (tool, server), `MaxIterationsError` (iterations). Budget guard in `Agent.run()` now throws `BudgetExceededError`. `runWithRetry()` uses `instanceof` checks for non-retryable errors. All exported from package index.
- [x] ~~**Core Module Test Coverage**~~ — ✅ DONE. New `src/core.test.ts` with 29 tests covering 4 previously untested modules: Error Types (8 tests — inheritance, properties, retryable logic), ConversationMemory (8 tests — CRUD, pruning, serialization, context string), AuditLog (7 tests — circular buffer, query filtering, stats aggregation), Pricing (5 tests — model matching, cost calculation, char heuristic). Total suite: 84 tests, 0 failures across 8 files.
- [x] ~~**README Rewrite**~~ — ✅ DONE. Complete rewrite documenting all 20+ features: Agent API (run, runBatch, runWithRetry, clone, estimateCost, middleware), LoopAgent (SSE, checkpointing, serialization), Cost Controls (budget guard, token tracking, pricing API), Memory, Cache, Fallback, Errors, Audit, Gemini Multimodal, x402 Payments, Analytics. Full export list. Test badge updated to 84 tests. Fixed broken markdown (unclosed code block on line 231).
- [x] ~~**Agent Run Telemetry Events**~~ — ✅ DONE. `RunEvent` discriminated union with 7 event types: `run_start`, `llm_call`, `llm_complete`, `tool_start`, `tool_complete`, `run_complete`, `run_error`. Each carries agentName, timestamp, and type-specific metadata (requestId, duration, usage, cost, tool/server names). `agent.onEvent(callback)` registers a non-fatal callback. Events emitted at 5 wiring points in `run()`. `clone()` preserves callback. 4 new tests (88 total, 0 failures). Types exported from package index.

- [x] ~~**Agent Timeout Guard**~~ — ✅ DONE. `TimeoutError` class in `src/errors.ts` (carries `durationMs`, `maxDurationMs`). `maxDurationMs` field on `AgentConfig`. `run()` uses `Promise.race()` against a timer when configured. Non-retryable in `runWithRetry()`. Exported from package index. 1 new test (89 total, 0 failures).
- [x] ~~**Agent Context Window Tracking**~~ — ✅ DONE. New `src/context.ts` with `ContextTracker` class and `getContextWindowSize()`. Model-specific context windows (GPT-4: 8K, GPT-4o: 128K, Claude: 200K, Gemini: 1M). 4-level warning system (ok/warning@75%/critical@90%/exceeded@100%). Auto-tracked in Agent via `contextTracker.addUsage()` after each `run()`. Exposed via `agent.contextUsage` getter and `agent.resetContext()`. 6 new tests (95 total, 0 failures). Exported from package index.
- [x] ~~**OpenTelemetry Integration**~~ — ✅ DONE. New `src/otel.ts` with zero-dependency OTLP span exporter. `createOtelCallback(config)` returns a `RunEventCallback` for `agent.onEvent()`. Converts `run_start`/`run_complete`/`run_error`/`tool_complete` events into OTLP spans with attributes (agent name, LLM, tokens, cost, errors). Supports batching, custom headers, resource attributes. Spans POSTed to any OTLP-compatible collector. 3 new tests (106 total, 0 failures). Exported from package index.
- [x] ~~**Agent Streaming Response**~~ — ✅ DONE. `StreamChunk<T>` discriminated union type (progress update vs final result). `agent.runStream(input)` async generator yields progress chunks during execution, then a final chunk with `done: true` containing output or error. Uses internal queue + Promise-based wakeup for zero-polling. 3 new tests. Type exported from package index.
- [x] ~~**Agent Conversation Fork**~~ — ✅ DONE. Added `fork(branchLabel?)` method to `ConversationMemory`. Creates an independent deep copy of messages and config, enabling A/B testing of conversation paths. Auto-generates label if none provided. Added `label` field. 3 new tests (113 total, 0 failures).
- [x] ~~**Cost Dashboard API**~~ — ✅ DONE. New `src/cost-tracker.ts` with `CostTracker` class. Records `CostRecord` per run (agent, model, tokens, cost, status). `getSummary(sinceMs?)` returns aggregations by model and agent, success/error counts, averages. Global `costTracker` singleton. Configurable max records (default 10K). Serialization support. 4 new tests. Exported from package index.
- [x] ~~**Agent Response Caching**~~ — ✅ DONE. Added `cacheConfig` option to `AgentConfig` (ttlMs, maxEntries). Agent routes through `cachedCallLLM` when set, using existing LRU cache with FNV-1a hashing. Private `callLLMCached()` helper method. 3 new tests (116 total, 0 failures).
- [x] ~~**Agent Batch Concurrency Control**~~ — ✅ DONE. Enhanced `runBatch()` with `delayBetweenBatchesMs` (rate limiting delay between chunks) and `onBatchProgress(completed, total, errors)` callback for tracking batch progress. Prevents overwhelming API rate limits on large datasets.
- [x] ~~**Agent Output Validation Hooks**~~ — ✅ DONE. Added `outputValidators` to `AgentConfig` — array of `OutputValidator` functions that receive raw LLM output and can throw to reject. Wired into `generateResponse()` before JSON/XML parsing. Type exported from package index. 2 new tests (123 total, 0 failures).
- [x] ~~**Agent Prompt Templates**~~ — ✅ DONE. New `src/templates.ts` with `createTemplate()` for `{{var:default}}` interpolation, `composeTemplates()` for composition, and 5 built-in `TEMPLATES` (summarize, classify, extract, translate, codeReview). Required variable validation throws on missing vars. 5 new tests. Exported from package index.
- [x] ~~**Agent Memory Summarization**~~ — ✅ DONE. Added `summarize(keepRecent=5)` and `summarizeWithLLM(summarizer, keepRecent)` to `ConversationMemory`. Extractive summarize compresses older messages into a system summary message. LLM-based accepts custom async summarizer function. 3 new tests (131 total, 0 failures).
- [x] ~~**Agent Tool Authorization**~~ — ✅ DONE. New `src/tool-auth.ts` with `ToolAuthorizer` class. Whitelist/blacklist modes, `server:tool` granular rules, denied attempt logging, `addTool/removeTool` mutations. Factory functions: `allowAllTools()`, `onlyTools(...)`, `blockTools(...)`. 5 new tests. Exported from package index.
- [x] ~~**Agent Retry Strategies**~~ — ✅ DONE. New `src/retry.ts` with `linearRetry`, `exponentialBackoff`, `fullJitter`, `noRetry` strategy factories. Generic `withRetry(fn, strategy, onRetry)` executor with `shouldRetry` error filter. Supports jitter, delay caps, and configurable max retries. 5 new tests (141 total, 0 failures).
- [x] ~~**Agent Event Bus**~~ — ✅ DONE. New `src/event-bus.ts` with `EventBus` class. `on/once/emit/off/clear`, wildcard `*` listener, event history (capped at 100), `waitFor()` promise-based blocking with timeout, max listener warnings, `globalBus` singleton. 5 new tests. Exported from package index.
- [x] ~~**Agent Pipeline Composition**~~ — ✅ DONE. New `src/pipeline.ts` with `createPipeline().pipe().transform().run()`. `PipelineResult` includes step timing, intermediate outputs, total duration. `PipelineError` preserves completed steps on failure. `onStepComplete` callback. `fanOut()` for parallel execution. 5 new tests (149 total, 0 failures).
- [x] ~~**Agent Health Check**~~ — ✅ DONE. New `src/health.ts` with `healthCheck(agent)` returning `HealthReport`. Checks: LLM config, schema definition, system prompt, memory health, cost budget. Server connectivity probes with latency. Runtime metrics (heap, uptime, platform). `formatHealthReport()` for human-readable output. 3 new tests.
- [x] ~~**Agent Observability Dashboard**~~ — deferred to next sprint (needs browser-side HTML generation).
- [x] ~~**Agent Schema Evolution**~~ — ✅ DONE. New `src/schema-evolution.ts` with `createSchemaEvolution()` builder. Versioned Zod schemas with `up/down` migration chains. `migrate(data, fromV, toV)` for multi-step bidirectional migration. `validate(data, version)` checks against any version. 5 new tests (154 total, 0 failures). Exported from package index.
- [x] ~~**Agent Middleware Ordering**~~ — ✅ DONE. New `src/middleware-order.ts` with `MiddlewareChain` class. Named middleware with `before`/`after` dependency constraints. Kahn's topological sort. Circular dependency detection. `enabled: false` toggle. Koa-style `next()` execution. 5 new tests (164 total, 0 failures).
- [x] ~~**Agent Input Preprocessors**~~ — ✅ DONE. New `src/preprocessors.ts` with `chainPreprocessors()` for composition. Built-ins: `trimStrings()`, `validateLength({ maxLength, truncate })`, `addTimestamp()`, `stripFields(...)`, `withDefaults(obj)`, `renameFields(mapping)`, `customPreprocessor(fn)`. 5 new tests.
- [x] ~~**Agent Rate Limiter**~~ — ✅ DONE. New `src/rate-limiter.ts` with `RateLimiter` class. Token bucket with `acquire()` (blocking), `tryAcquire()` (non-blocking), auto-refill timer (100ms), burst capacity, queue-based waiting with `maxQueueSize` rejection. Stats: available tokens, total acquired/rejected, queue length. `reset()` and `destroy()`. 5 new tests (174 total, 0 failures).
- [x] ~~**Agent Context Window Manager**~~ — ✅ DONE. New `src/context-window.ts` with `ContextWindow` class. Token estimation (~4 chars/token), auto-pruning (FIFO or summarize strategy), pinned messages (system prompts), reserve budget for output. `fits(content)` check, `clearUnpinned()`, utilization stats (0-1). 5 new tests.
- [x] ~~**Agent Structured Logging**~~ — ✅ DONE. New `src/structured-log.ts` with `StructuredLogger` class. JSON log records with levels (debug/info/warn/error/fatal), correlation IDs for request tracing, child loggers with field inheritance. Pluggable transports: `consoleTransport()`, `jsonTransport()`, `bufferTransport()`. Record buffering with rotation (500). `getRecords(level)` filter. 5 new tests (184 total, 0 failures).
- [x] ~~**Agent Config Profiles**~~ — ✅ DONE. New `src/config-profiles.ts` with `ConfigProfileManager` class. Named profiles with `register(name, config)`, `setActive()`, `getActive()`. Base config inheritance via `setBase()`. `autoSelect(env)` matches `NODE_ENV`. `withOverrides()` for temporary changes. `listProfiles()` metadata. 5 new tests.
- [x] ~~**Agent Response Cache**~~ — ✅ DONE. New `src/response-cache.ts` with `ResponseCache` class. LRU eviction, TTL expiry, deterministic `hash()` for inputs, `getOrSet()` compute-on-miss, `prune()` expired entries. Stats: hits, misses, hit rate, evictions. 5 new tests (194 total, 0 failures).
- [x] ~~**Agent Batch Processor**~~ — ✅ DONE. New `src/batch-processor.ts` with `batchProcess()`. Semaphore-based concurrency control. `continueOnError`, `onProgress`/`onError` callbacks, results sorted by index. `chunk()` utility. `sequentialProcess()` convenience. 5 new tests.
- [x] ~~**Agent Dependency Injection**~~ — ✅ DONE. New `src/di.ts` with `DIContainer` class. Singleton/transient lifetimes, `value()` for constants, `createScope()` for child containers with parent resolution chain, tag-based `getByTag()` lookup, `tryResolve()` for optional deps, `reset()` singletons. 5 new tests (204 total, 0 failures).
- [x] ~~**Agent Output Formatters**~~ — ✅ DONE. New `src/output-formatters.ts` with `formatOutput(data, format)`. Built-in formats: JSON (pretty), Markdown (headers + bold), HTML (tables + lists), plain text, YAML. Custom formatter via function. `templateFormatter('{{field}}')` for string interpolation. HTML escaping. 5 new tests.
- [x] ~~**Agent Guardrails**~~ — ✅ DONE. New `src/guardrails.ts` with `Guardrails` class. Configurable rules with reject/warn/sanitize actions. Violation tracking with rule name, action, message. Enable/disable per rule. Built-in rules: `maxLengthRule()`, `noPIIRule()` (SSN/CC/email), `blockKeywords()`, `nonEmptyRule()`. 5 new tests (214 total, 0 failures).
- [x] ~~**Agent Session Manager**~~ — ✅ DONE. New `src/session.ts` with `SessionManager` class. Key-value state with auto-save, TTL expiry, `snapshot()`, `serialize()`/`restore()`, session merging. Custom serializers. Version tracking. Global storage persistence. 5 new tests.
- [x] ~~**Agent Prompt Templates**~~ — ✅ DONE. New `src/prompt-templates.ts` with `createTemplate()`. `{{variable}}` interpolation, `{{#if}}` / `{{#unless}}` conditionals, `{{#each}}` loops with `_item`/`_index`. `composeTemplates()` for multi-section prompts. `systemPrompt()` / `userPrompt()` helpers. Variable extraction. Internal `_render()` preserves whitespace. 5 new tests (224 total, 0 failures).
- [x] ~~**Agent State Machine**~~ — ✅ DONE. New `src/state-machine.ts` with `StateMachine` class. Typed transitions with guard conditions, async `onEnter`/`onExit` handlers, `canSend()`, `availableEvents`, `transitionHistory`, `snapshot()`, `reset()`, `is()` state check. 5 new tests.
- [x] ~~**Agent Conversation Memory**~~ — ✅ DONE. New `src/conversation-memory.ts` with `ConversationMemory` class. Jaccard-based text similarity search with tag bonus and importance weighting. `getByTag()`, `getRecent()`, `getImportant()`. Auto-pruning by importance. `serialize()`/`restore()`, `import()`/`export()`. 5 new tests (234 total, 0 failures).
- [x] ~~**Agent Tool Registry**~~ — ✅ DONE. New `src/tool-registry.ts` with `ToolRegistry` class. `register()`/`deregister()`, `invoke()` with count tracking, enable/disable, search by name/description, filter by category/tag. `list()` with full metadata. 5 new tests.
- [x] ~~**Agent Pipeline Composer**~~ — ✅ DONE. New `src/pipeline-composer.ts` with `PipelineComposer` class. Typed `pipe()` chaining, before/after hooks, per-step error handling (skip/abort/fallback), execution timing, `PipeResult` with step durations and success tracking. `compose()` factory. 5 new tests (244 total, 0 failures).
- [x] ~~**Agent Metrics Collector**~~ — ✅ DONE. New `src/metrics.ts` with `MetricsCollector` class. `record()` histogram values, `increment()` counters, `getStats()` with min/max/avg/p50/p95/p99, async `time()` helper, `summary()` for all metrics, `export()` for persistence. 5 new tests.
- [x] ~~**Agent Webhook Handler**~~ — ✅ DONE. `WebhookHandler` class with HMAC-SHA256 signature verification (Web Crypto API), event routing, wildcard handlers, payload size limits, event allowlists, and history tracking. Supports GitHub `sha256=` prefix. Timing-safe string comparison prevents timing attacks. `hmacSha256()` utility exported. 7 new tests (261 total, 0 failures).
- [x] ~~**Agent Schema Validator**~~ — ✅ DONE. Runtime schema validation with `StringField`, `NumberField`, `BooleanField`, `ArrayField`, `ObjectSchema`. Deep path error reporting. Factory functions: `string()`, `number()`, `boolean()`, `array()`, `object()`. Optional fields, min/max, pattern, oneOf, integer. 5 existing tests.

## 🟢 Priority: Next Steps
- [x] ~~**Agent WebSocket Client**~~ — ✅ DONE. `AgentWebSocketClient` implemented with Native Bun WebSocket, auto-reconnect, ping/pong heartbeat, fire-and-forget notifications, and JSON-RPC 2.0 message framing routing via Promises. Exported natively from the core `index.ts`.
- [x] ~~**Agent File System Tools**~~ — ✅ DONE. Exported built-in MCP tool definitions (`read_file`, `write_file`, `list_dir`, `search_files`) with strict `path.resolve` directory sandboxing. Includes helper `serveFileSystemMCP` to instantly spawn a local Bun JSON-RPC / HTTP MCP Server for Agents.
- [x] ~~**Agent Dashboard Web UI**~~ — ✅ DONE. Added `serveAgentDashboard()` which fires up a standalone HTTP server exporting a stunning glassmorphic UI. Automatically hooks into `costTracker` for live metric aggregations and uses SSE to stream `RunEvents` out of the global `EventBus` directly into the browser. Tests verify rendering and SSE framing.

## 🟢 Priority: Next Steps (Backlog Replenishment)
- [ ] **Anthropic Prompt Caching** — Implement native support for Claude's `cache_control` blocks. Auto-tag the `ConversationMemory` boundaries and large `SystemPrompt` static sections to reduce repetitive token costs for long-running Loops by over 80%.
- [ ] **Agent Isolated Code Sandbox** — Build a new built-in MCP Tool that spawns heavily restricted isolated JavaScript/TypeScript execution contexts (via WebWorkers or Bun `vm` isolation), allowing the LLM to safely generate, run, and self-correct actual code iteratively.
- [x] ~~**Deterministic Mock Engine**~~ — ✅ DONE. Added a dedicated `AgentMock` interface (`mock-engine.ts`) with sequence-based deterministic matching, simulated latency, and tool completion telemetry hook bypassing the real LLM endpoint via an unhandled early-exit interceptor flow handling `MockAbortedExecution`.
